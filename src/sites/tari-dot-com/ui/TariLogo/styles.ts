'use client';

import styled from 'styled-components';

export const Wrapper = styled.div`
    width: 121px;
    flex-shrink: 0;
    transition: width 0.3s ease;

    svg {
        width: 100%;
    }
`;
